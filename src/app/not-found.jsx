'use client';

/**
 * NotFoundPage Component
 *
 * This component displays a "Not Found" message to indicate that the requested page
 * could not be found. It is typically used for 404 error pages.
 *
 * @returns {JSX.Element} The rendered "Not Found" page with a header.
 */
export default function NotFoundPage() {
   return <h1>Not found</h1>;
}
