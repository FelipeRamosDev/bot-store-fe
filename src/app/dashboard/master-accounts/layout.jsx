export const metadata = {
   title: "Master Accounts - BotStore Dashboard",
   description: "Look after for your master accounts.",
}

export default function MasterAccountsLayout({ children }) {
   return children;
}
